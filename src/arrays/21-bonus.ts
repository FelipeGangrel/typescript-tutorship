/**
 * For of loop
 */

const numbers = [1, 2, 3, 4, 5];

/**
 * We can iterate over the elements of an array using a for of loop
 */

for (const number of numbers) {
  console.log(number); // 1, 2, 3, 4, 5
}

export {};
